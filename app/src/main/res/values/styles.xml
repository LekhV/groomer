<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorGroom</item>
        <item name="colorPrimaryDark">@color/colorGroom</item>
        <item name="colorAccent">@color/colorGroom</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/colorGroom</item>

        <item name="tabSelectedTextColor">@color/colorWite</item>
        <item name="tabTextAppearance">@style/MyCustomTextAppearance</item>
    </style>

    <style name="MyCustomTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">20dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="textAllCaps">false</item>
    </style>

    <style name="colorsToolbar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorWite</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textColorSecondary">@color/colorWite</item>
        <item name="android:textColor">#2dbbd2</item>
        <item name="android:textAllCaps">false</item>
        <item name="actionMenuTextColor">#2dbbd2</item>
        <item name="android:actionMenuTextColor">#2dbbd2</item>
        <item name="android:windowActionBar">false</item>

    </style>
    <style name="NavigationDrawerStyle">
        <item name="android:textSize">18dp</item><!-- text size in menu-->
    </style>
    <style name="StyleFull" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


</resources>
